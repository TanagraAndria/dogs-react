import React from 'react';
import styles from '../header/Header.module.css';

export const Header = () => {
    return (
        <div  className={styles.header}>
            Header
        </div>
    )
}
